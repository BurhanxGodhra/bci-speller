# --- Core signal processing / ML for EEG ---
mne
moabb
pyriemann
scikit-learn
numpy>=1.24.0,<2.0.0
scipy

# --- Hardware / streaming ---
pylsl==1.16.2

# --- Rendering (SSVEP flicker + P300 flash matrix) ---
pygame==2.6.0

# --- OS-level keypress injection (Phase 7) ---
pyautogui==0.9.54
pyobjc-core==10.3.1        # required by pyautogui on macOS
pyobjc-framework-Quartz==10.3.1
pyobjc-framework-Cocoa==10.3.1   # NSScreen display refresh-rate query (Phase 1 diagnostic)

# --- Config / HAL ---
pyyaml==6.0.2

# --- Local LLM predictive text (Phase 6) ---
transformers==4.44.2
torch==2.4.1                # Apple Silicon: installs with MPS backend support automatically
accelerate==0.33.0
sentencepiece==0.2.0

# --- Utilities ---
pandas
matplotlib
tqdm
